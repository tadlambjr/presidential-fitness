<nav class="bg-red-700 text-white shadow-lg">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <div class="flex items-center space-x-2">
        <div class="w-8 h-2 bg-red-600"></div>
        <div class="w-8 h-2 bg-white"></div>
        <div class="w-8 h-2 bg-blue-800"></div>
        <%= link_to root_path, class: "ml-4 text-xl font-bold hover:text-red-200 transition-colors" do %>
          Presidential Fitness
        <% end %>
      </div>

      <div class="flex items-center space-x-6">
        <% if logged_in? %>
          <%= link_to "Dashboard", metrics_path, class: "hover:text-red-200 transition-colors" %>
          <%= link_to "Logout", logout_path, data: { turbo_method: :delete }, class: "hover:text-red-200 transition-colors" %>
        <% else %>
          <%= link_to "Sign In", login_path, class: "hover:text-red-200 transition-colors" %>
          <%= link_to "Sign Up", signup_path, class: "bg-white text-red-700 px-4 py-2 rounded-lg font-semibold hover:bg-red-100 transition-colors" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
